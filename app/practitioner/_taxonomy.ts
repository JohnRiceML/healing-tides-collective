// Promoted to app/_lib/taxonomy.ts so the editor AND the public directory/filters
// share one source. Kept here as a thin re-export so existing editor imports
// (`./_taxonomy`) keep working. Still PLACEHOLDER config — awaits Nora's real,
// admin-owned taxonomy (brief §3d).
export * from "@/app/_lib/taxonomy";
